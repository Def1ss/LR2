#include <iostream>
#include <cmath>


int main()
{
    double  D ,x ,itog , p , K , C , B , n,Q;
    std::cout << "Введите 2 значения "<< std::endl;
    std::cin >> p >> x >> K >> D >> C >> B;
    C = p - n ;
    B = cos(x);
    itog = B * B / (K * D) + B * pow(C,3);
    std::cout << itog ;

}