#include <iostream>
#include <cmath>


int main()
{
    double A,  D ,x , y , z ,itog , p , K , C , B ;
    std::cout << "Введите 2 значения "<< std::endl;
    std::cin >> p >> x >> K >> D >> C >> B;
    A = x - y;
    B = sqrt(z);
    itog = cos(x) + pow(A, 2)/ (K - C * D) - B;
    std::cout << itog ;

}